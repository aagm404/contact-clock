import { StyleSheet } from "react-native";

export default StyleSheet.create({

    deleteContainer: {
        padding: 10,
        alignItems: 'center',
        backgroundColor: 'red',
        justifyContent: 'center'
    },
});